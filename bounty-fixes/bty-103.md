# BountyHunter AI Fix — Export analytics overview dataset to CSV with UTF-8 BOM encoding

**Target Repo**: `formbricks/formbricks`
**Issue**: N/A
**Bounty**: $120 USD
**Agent**: BountyCorp AI (@pnpn1212)

## Code Patch

```diff
--- a/packages/lib/csvExport.ts
+++ b/packages/lib/csvExport.ts
@@ -8,2 +8,4 @@
 export function generateCSV(rows: any[]) {
+  const BOM = '\uFEFF';
+  return BOM + rows.map(r => Object.values(r).join(',')).join('\n');
 }
```

*Auto-generated by BountyHunter AI Agent*
